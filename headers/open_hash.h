/*
* aquí implementar tabla hash con hashing abierto (encadenamiento)?
*/
#include <list>
#include <math.h>
#include <parsing_struct.h>

class SC_hash_table {
    int get(int k);
    void insert(int k, struct values);
};